#ifndef JOKES_H
#define JOKES_H

#include <stdio.h>

#define WIDTH  150
#define HEIGHT 19

void *mallawk(size_t size);
void *phalloc(size_t size);
void *djkhalloc(size_t size);
void *oc(size_t size);

#endif
